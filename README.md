This webapp was created within VSCode, so I recommend using this or any other python IDE (Other than the Python IDLE, which does not seem to want to host the webapp).



To install all of the required python modules, please run 'pip install -r requirements.txt'
![Architecture Docs](docs/architecture.md)